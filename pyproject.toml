[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "evaluator-result-parser"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "numpy",
    "matplotlib",
    "Pillow",
]

[project.optional-dependencies]
server = [
    "fastapi>=0.100",
    "uvicorn[standard]>=0.23",
]

[project.scripts]
t4-batch = "t4_visualizer.batch:main"
t4-visualize = "t4_visualizer.visualize:main"
t4-inspect = "t4_visualizer.inspect:main"
t4-cache = "t4_visualizer.downloader:cache_main"
t4-multi = "t4_visualizer.batch:multi_main"
t4-server = "t4_visualizer.server:main"
t4-load-test = "t4_visualizer.load_test:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["t4_visualizer*", "result_parser*"]

# HTML templates and static assets are loaded at runtime via
# Path(__file__).parent / "templates" (and "static") — see server.py.
[tool.setuptools.package-data]
t4_visualizer = [
    "templates/*.html",
    "static/*",
    "static/viewer_three/*",
    "static/viewer_three/jsm/controls/*",
    "static/viewer_three/jsm/loaders/*",
    "static/viewer_three/jsm/utils/*",
    "static/viewer_three/jsm/environments/*",
    "static/viewer_three/app/*",
]
